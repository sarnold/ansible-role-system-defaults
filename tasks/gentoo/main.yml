---

# install default packages

- name: install default packages
  become: yes
  portage: package="{{ item }}" state="present" newuse=yes update=yes
  with_items:
    - app-editors/vim
    - dev-vcs/git
